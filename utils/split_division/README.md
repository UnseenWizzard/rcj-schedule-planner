`split_division.sh` takes an input csv that lists all teams in "TeamName,Division" format, and produces separate output files that can be used for the scheduler.

Outfiles will be `<division>.sh` and contain a csv style list of just the team_name.